export const checkCredentials = (email, password) => {
    const validEmail = 'jesusayllonpacheco@gmail.com';
    const validPassword = 'password1234';
    return email === validEmail && password === validPassword;
  };
  